# Python dependencies for the Meditech Lab host-side scripts.
#   pip install -r requirements.txt
#
# (The five services themselves run in Docker; these libraries are only
#  needed by the helper scripts that load data and drive the UIs.)

flask>=3.0
requests>=2.31
boto3>=1.34
urllib3>=2.0

# Only needed for module2_hands_on_exercise.py (standalone teaching exercise):
pandas>=2.0
tabulate>=0.9
colorama>=0.4

# Only needed if you want to regenerate the PNG diagrams in lab_visualizations/:
matplotlib>=3.7
numpy>=1.24
