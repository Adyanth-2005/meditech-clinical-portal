# Advanced platform dependencies.
#   pip install -r requirements-advanced.txt

# ── Phase 1: RBAC gateway + login UI ──
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
PyJWT>=2.8
bcrypt>=4.1
psycopg2-binary>=2.9     # talks to the lab's PostgreSQL

# ── Phase 2/3: Milvus + hybrid RAG (installed when you reach those phases) ──
pymilvus>=2.4            # Milvus client
elasticsearch>=8.10,<9   # BM25 half of hybrid retrieval (matches ES 8.10.2)
requests>=2.31           # Ollama HTTP API

# ── tests ──
pytest>=8.0
httpx>=0.27              # FastAPI TestClient dependency
boto3
python-multipart
